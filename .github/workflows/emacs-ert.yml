name: Emacs ERT Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  ert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Emacs (nox)
        run: |
          sudo apt-get update
          sudo apt-get install -y emacs-nox

      - name: Run workflow ERT tests
        run: |
          emacs -Q --batch \
            --eval "(provide 'gptel)" \
            -l org \
            -l private_dot_config/doom/lisp/tests/my-gptel-org-workflow-tests.el \
            -f ert-run-tests-batch-and-exit
      - name: Run edit tool ERT tests
        run: |
          emacs -Q --batch \
            -l org \
            -l private_dot_config/doom/lisp/tests/my-gptel-tools-edit-tests.el \
            -f ert-run-tests-batch-and-exit
      - name: Run task workflow ERT tests
        run: |
          emacs -Q --batch \
            -l org \
            -l private_dot_config/doom/lisp/tests/my-task-workflow-tests.el \
            -f ert-run-tests-batch-and-exit
