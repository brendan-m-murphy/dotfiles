# Exit early if not an interactive shell
case $- in
  *i*) ;;
  *) return ;;
esac

# Now load your modular config
if [ -d "$HOME/.bash.d" ]; then
  for file in "$HOME/.bash.d"/*.sh; do
    [ -r "$file" ] && source "$file"
  done
fi
